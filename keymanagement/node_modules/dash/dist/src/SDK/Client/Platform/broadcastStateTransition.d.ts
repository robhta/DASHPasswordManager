import { Platform } from "./Platform";
/**
 * @param {Platform} platform
 * @param stateTransition
 * @param identity
 * @param {number} [keyIndex=0]
 */
export default function broadcastStateTransition(platform: Platform, stateTransition: any, identity: any, keyIndex?: number): Promise<void>;
